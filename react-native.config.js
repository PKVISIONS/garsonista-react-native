/* eslint-disable @typescript-eslint/no-var-requires */
/** Links `src/assets/fonts` into iOS + Android (run `npx react-native-asset` after adding fonts). */
module.exports = {
  project: {
    android: {},
    ios: {},
  },
  assets: ['./src/assets/fonts/'],
};
